"use client";
import SkyFare from './skyfare/SkyFare';  // Ensure the path matches the location of the SkyFare component

const About = () => {
  return (
    <div>
      <h1></h1>
      <SkyFare />  {/* Render the SkyFare component */}
    </div>
  );
};

export default About;
